{
    "scripts": {
        "start": "node app.js",
        "setup": "psql -U postgres -f setup.sql",
        "test": "jest"
    },
    "dependencies": {
        "pg": "^8.11.0",
        "express": "^4.18.0"
      },
    "devDependencies": {
        "jest": "^29.7.0",
        "supertest": "^7.0.0"
    }
}
